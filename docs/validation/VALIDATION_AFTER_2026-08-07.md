# 验证清单 — 2026-08-07 深度修复

> 本文件由并行代理分别追加各自章节：内存加固 / 余额查询 / 课程总览交互。P0 必测，P1 重要，P2 可选。

## 一、内存加固（Memory Hardening）

### P0（必须）

- [ ] `npm run typecheck` 通过（已在本次修复后验证 ✅）
- [ ] Electron 启动后启动/停止任务，任务管理器中 `python.exe` 与 `chromium.exe` 进程数回归基线（无残留累积）
- [ ] 将 Python 路径配置为不存在值后启动任务，应出现“找不到 Python 解释器”友好报错，且任务状态为 error（不再卡在 running），再次启动新任务不被“已有任务运行中”拒绝

### P1（重要）

- [ ] 连续启动并停止 25+ 个任务（可 mock/快速失败），主进程内存不随任务数线性增长（jobs Map 上限 20 生效）
- [ ] 后端异常刷屏输出（无换行）时，PythonBridge stdout 缓冲被截断并仅告警一次，应用不卡死
- [ ] 退出应用后任务管理器无遗留 `chromium.exe` / `python.exe`

### P2（可选）

- [ ] 长任务 2 小时以上运行，RAM 占用曲线平稳（配合后端 RAM guard 观察）
- [ ] 抽查 `data/temp/_residue-*` 不再新增（当前为测试残留，非应用产生）

## 二、课程总览交互（Atlas Scan UX）

### P0（必须）

- [ ] 新账号列表 → 「一键扫描」完成后，选中课程，底部操作栏**不再出现「仅扫描」**按钮
- [ ] 所选账号中存在未扫描账号时，「仅扫描」出现且只对未扫描账号发起扫描（可通过后端日志确认 `--accounts` 只含未扫描账号）
- [ ] 任务运行中，底部四个操作按钮与左侧「一键扫描」均为禁用态，无法重复启动
- [ ] `npm run typecheck` 通过（已在本次修复后验证 ✅）

### P1（重要）

- [ ] 扫描过但课程为空的账号再次扫描后，课程列表刷新且按钮状态正确
- [ ] 混选“已扫描账号 + 未扫描账号”时，操作栏摘要（N 门课程 · M 个账号）与按钮语义一致

### P2（可选）

- [ ] 扫描进行中（`courseStore.scanning`）时左侧账号徽标/按钮状态无闪烁
- [ ] 从课程总览跳转执行工作室再返回，选择状态保持且「仅扫描」显隐正确
